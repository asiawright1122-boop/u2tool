# Requirements: v0.0.36 - TDK Cleanup Batch Four

## Milestone Goal

继续使用 v0.0.32 的 TDK cleanup candidate export，完成第四组可审查的实际文案收敛：处理 fresh v0.0.36 candidate JSON 中 ranks `1` 到 `10` 的 `root_base_match`、拉丁语系、overlong `seo_description` 候选，同步更新 root/base 两层，把 warning-only TDK debt 从 `2762` 降到 `2752`。

This is another **bounded metadata cleanup** milestone. It should not redesign validators, change thresholds, or mass-edit multilingual copy. The expected outcome is a measured 10-warning reduction while preserving `0` hard errors and synchronized root/base metadata.

## Motivation

- **The cleanup loop is still repeatable**: the previous three batches each removed 10 warning-only findings without validator changes.
- **The next candidate JSON remains low-risk**: the selected rank `1` through `10` rows are all `root_base_match` candidates in `es`, `fr`, and `pt`.
- **Warning debt should keep moving downward**: this batch keeps the candidate queue actionable while preserving hard-error semantics.
- **Evidence-led tie handling remains important**: same-overrun candidates can display differently in CLI summaries, so this milestone follows the fresh candidate JSON rank order.

## Requirements

### TDK Cleanup Batch Four (TDK-CBF)

- [x] **TDK-CBF-01** - **Fresh candidate selection evidence**: Capture the v0.0.36 baseline and top candidate list: `0` hard errors, `2762` warnings, with the selected 10 candidates recorded by locale, slug, length, overrun, and source-layer status.
- [x] **TDK-CBF-02** - **Bounded root/base synchronized edits**: Update exactly the selected 10 effective `seo_description` entries in both root and base message layers, preserving synchronized values after edit.
- [x] **TDK-CBF-03** - **Safe-length topical descriptions**: Every replacement description must remain within locale safe bounds and describe the actual tool function without generic boilerplate.
- [x] **TDK-CBF-04** - **Warning reduction proof**: Re-run `validate:tdk-integrity` and record the warning count dropping from `2762` to `2752`, with hard errors staying at `0`.
- [x] **TDK-CBF-05** - **Focused sync verification**: Run a focused script/check that verifies all 10 edited `(locale, slug)` root/base values are identical and within bounds.
- [x] **TDK-CBF-06** - **Planning/testing doc alignment**: Update roadmap, state, phase baseline, milestone audit, and testing docs so the next batch can continue from the remaining candidate queue.

## Selected Batch

| Rank | Locale | Slug | Category | Length | Max | Over By | Source Status |
| :--- | :--- | :--- | :--- | ---: | ---: | ---: | :--- |
| 1 | es | typescript-playground | development | 380 | 180 | 200 | root_base_match |
| 2 | es | unit-price-calculator | math | 380 | 180 | 200 | root_base_match |
| 3 | fr | image-flip-rotate | image | 380 | 180 | 200 | root_base_match |
| 4 | fr | social-media-size-guide | image | 380 | 180 | 200 | root_base_match |
| 5 | fr | project-risk-analyzer | office | 380 | 180 | 200 | root_base_match |
| 6 | pt | base64-image-converter | image | 380 | 180 | 200 | root_base_match |
| 7 | es | concrete-calculator | math | 379 | 180 | 199 | root_base_match |
| 8 | es | http-status-code-reference | network | 379 | 180 | 199 | root_base_match |
| 9 | es | calendar-availability-finder | office | 379 | 180 | 199 | root_base_match |
| 10 | es | paraphrase-tool | text | 379 | 180 | 199 | root_base_match |

## Out of Scope

- **JA/KO/RU/AR cleanup** — these require stronger language review and stay out of this batch.
- **More than 10 metadata edits** — keep the batch auditable.
- **Validator behavior changes** — candidate export and TDK thresholds stay as-is.
- **Rendered metadata architecture changes** — source copy only.
- **Duplicate-key cleanup** — some locale `base.json` files contain legacy duplicate slug blocks; this batch edits only the effective values surfaced by the validator.

## Key Design Constraints

1. **Edit both layers together**: every selected root/base pair must end with identical values.
2. **Stay within bounds**: Latin `seo_description` must be between `50` and `180` characters.
3. **Keep copy specific**: descriptions should mention the tool's actual job, not only privacy/free boilerplate.
4. **Measure before and after**: record the exact warning-count delta.
5. **Leave reports ignored**: `.planning/research/reports/` remains untracked.

## Traceability

| Requirement ID | Description | Assigned Phase | Status |
| :--- | :--- | :--- | :--- |
| **TDK-CBF-01** | Fresh candidate selection evidence | Phase 96 | Complete |
| **TDK-CBF-02** | Bounded root/base synchronized edits | Phase 96 | Complete |
| **TDK-CBF-03** | Safe-length topical descriptions | Phase 96 | Complete |
| **TDK-CBF-04** | Warning reduction proof | Phase 96 | Complete |
| **TDK-CBF-05** | Focused sync verification | Phase 96 | Complete |
| **TDK-CBF-06** | Planning/testing doc alignment | Phase 96 | Complete |

## Fresh Baseline Evidence

- Baseline command: `npm run validate:tdk-integrity -- --top 10 --report-path .planning/research/reports/tdk-integrity-v0.0.36-candidate-baseline.json --candidates-path .planning/research/reports/tdk-cleanup-candidates-v0.0.36-top30.json --candidate-top 30 --candidate-fields seo_description --candidate-directions long`
- Baseline result: PASS with `0` errors, `2762` warnings, and `30` exported cleanup candidates.
- Selected batch: candidate JSON ranks `1` through `10`, all `root_base_match`, all overlong `seo_description`, all Latin-script locales (`es`, `fr`, `pt`).
