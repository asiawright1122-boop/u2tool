# Requirements Archive: v0.0.14 Production Release and GSC Recovery Measurement

**Status:** ACTIVE

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements: v0.0.14 - Production Release and GSC Recovery Measurement

## Milestone Goal

Recover organic traffic by moving the completed v0.0.13 recovery work from local evidence to live, crawlable production pages, then measuring whether Google Search Console cohorts regain impressions and clicks.

## Evidence Base

- v0.0.13 closed with 0 catalog `PopularUtilityTool` entries and green local release gates.
- Live route contract recheck on 2026-06-09: `npm run validate:production-routes` passed against `https://www.u2tool.com`; historical soft-404 and `${BASE_URL}` JSON-LD failures are no longer the active blocker.
- Live spot check on 2026-06-09: `passport-photo-maker`, `csv-to-vcard-converter`, and `vcard-to-csv-converter` return HTTP 200 and render real tool/support content.
- PR #25 was merged on 2026-06-09 and deployed successfully through Cloudflare workflow run 27190356884 at main commit `4a66929f3b1a337a0949d6ab2562da133f11cfb1`.
- Live category spot check on 2026-06-09: the newly added English `finance`, `generators`, and `lifestyle` category-support copy is visible on production.
- The worktree is heavily dirty with many unrelated changes; deployment must not publish unrelated local modifications by accident.

## Requirements

### Active

- [x] **R1** - Produce a deployable change manifest that separates v0.0.14 traffic-recovery release files from unrelated dirty worktree changes.
- [x] **R2** - Preserve the live route contract by keeping `npm run validate:production-routes` green before and after release.
- [x] **R3** - Ship the latest recovery slice safely: English `finance`, `generators`, and `lifestyle` category support content plus any compile-blocking fixes required for production build.
- [x] **R4** - Verify live pages after release: category support content appears on production, final converted tool URLs remain HTTP 200, and no `PopularUtilityTool` or internal reasoning trace appears in rendered surfaces.
- [x] **R5** - Define and execute a GSC recovery cohort plan for repaired pages, including indexing/request guidance for final converter/image/category URLs and the earlier GSC loss pages.
- [ ] **R6** - Create a measurement report that compares post-release GSC impressions, clicks, CTR, and indexed/crawled state for the repaired cohorts at 7-day, 14-day, and 28-day checkpoints.

## Out of Scope

- Adding new tool slugs before the existing recovery cohorts are deployed and measured.
- Deploying the entire dirty worktree without a release manifest.
- Reopening the historical soft-404 root cause unless the live route gate regresses.
- Exposing internal reasoning, hidden prompts, scratchpads, or raw planning notes to frontend users.
