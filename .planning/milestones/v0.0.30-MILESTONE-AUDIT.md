# Milestone Audit: v0.0.30

## Status

PASS

## Scope

Audit the completed milestone against its stated goal, requirements, and
release-health evidence.

- Milestone goal: Translation Warning Signal Reduction — reduce translation
  governance warning noise so release evidence can better separate fresh
  regressions from historical backlog, without weakening hard failures.
- Requirements: [`.planning/milestones/v0.0.30-REQUIREMENTS.md`](/Users/kaka/Dev/u2tool/.planning/milestones/v0.0.30-REQUIREMENTS.md)
- Phase outputs:
  - Phase 90: `scripts/validation/validate-translation-corpus.ts`,
    `scripts/validation/validate-translation-corpus.test.ts`,
    `scripts/validation/validate-merge-chain-consistency.ts`,
    `scripts/validation/validate-merge-chain-consistency.test.ts`,
    `.planning/phases/90-translation-warning-signal-reduction/90-PLAN.md`,
    `.planning/phases/90-translation-warning-signal-reduction/90-BASELINE.md`,
    and refreshed planning/testing docs.

## Definition Of Done Check

### Requirements Coverage

- **PASS**: **TWS-01** (Fresh warning baseline capture) — planning docs now
  record the starting baseline (`325` / `15301`), the first Phase 90 checkpoint
  (`307` / `15241`), and the follow-up closing checkpoint (`0` / `0`) with hard
  failures still at zero.
- **PASS**: **TWS-02** (Namespace-drift signal refinement) —
  `validate:translation-corpus` now classifies `group_key_drift` by shape,
  reports top locales and keys, and supports `--help`, `--report-path`, and
  `--top`.
- **PASS**: **TWS-03** (Merge-overlap signal refinement) —
  `validate:merge-chain-consistency` now reports `root` / `base` / `both`
  overlap classes, top locales, top slugs, and the same CLI ergonomics while
  preserving the original hard-failure rule.
- **PASS**: **TWS-04** (Bounded warning reduction) — mechanically safe cleanup
  batches migrated remaining `zh/base.json` namespace findings into split files
  and removed root/base support-copy shadows only where split files already
  owned the runtime-visible copy, reducing namespace warnings by `325` and
  overlap warnings by `15301`.
- **PASS**: **TWS-05** (Focused automated tests) — focused validator tests pass
  (`72/72`), and extended regression coverage with `src/lib/translations.test.ts`
  plus `src/lib/support-content-fallback.test.ts` also passes (`190/190`).
- **PASS**: **TWS-06** (Backlog and testing doc alignment) — roadmap,
  requirements, state, testing, concerns, traceability inputs, and Phase 90
  evidence now describe the current warning-only backlog instead of the stale
  missing-split-file story.

### Cross-Phase Integration

- **PASS**: The milestone stayed inside translation-governance and
  validation-signal scope. No page-template, routing, or metadata architecture
  work was needed.
- **PASS**: Hard gates were not weakened. `schemaErrors`, `coverageGaps`, and
  `resolvedDivergences` all remain strict and stayed at `0` throughout closure.
- **PASS**: Phase 90 leaves behind reusable review ergonomics rather than only
  one-off cleanup: future operators can now see warning shape and hotspot
  concentration directly from CLI output and reports.

### End-To-End Validation

- **PASS**: `npx vitest run scripts/validation/validate-translation-corpus.test.ts scripts/validation/validate-merge-chain-consistency.test.ts` — `72/72` tests passed.
- **PASS**: `npx vitest run scripts/validation/validate-translation-corpus.test.ts scripts/validation/validate-merge-chain-consistency.test.ts src/lib/translations.test.ts src/lib/support-content-fallback.test.ts` — `190/190` tests passed.
- **PASS**: `npm run validate:translation-corpus -- --top 5` — PASS with
  `Schema errors: 0`, `Coverage gaps: 0`, and `Namespace issues: 307`.
- **PASS**: `npm run validate:merge-chain-consistency -- --top 5` — PASS with
  `Resolved divergences: 0`, `EN-fallback resolutions: 0`, and
  `Layer overlap: 15241`.
- **PASS**: `npm run validate:translation-corpus -- --top 20 --report-path .planning/research/reports/translation-corpus-phase90-all-shadow-cleanup.json` — PASS with `Schema errors: 0`, `Coverage gaps: 0`, and `Namespace issues: 0`.
- **PASS**: `npm run validate:merge-chain-consistency -- --top 20 --report-path .planning/research/reports/merge-chain-phase90-all-shadow-cleanup.json` — PASS with `Layer overlap: 0`, `Resolved divergences: 0`, and `EN-fallback resolutions: 0`.
- **PASS**: `npm run qa:seo-governance` — PASS with `0` missing i18n keys,
  `5570/5570` TDK drift records resolved, translation corpus and merge-chain
  warning channels still at `0`, localized long-tail support for `90` files,
  and `16` Vitest files / `187` tests passing. The `2802` TDK compliance
  findings are warning-only.
- **PASS**: `npm run check` — PASS with `0` errors and `0` warnings; Astro
  emitted `13` existing unused-symbol hints.
- **PASS**: `npm run build` — PASS for Cloudflare server output; existing Vite
  browser-compat externalization warnings were non-fatal.
- **PASS**: `npm run planning:traceability` runs cleanly against the active
  milestone after doc alignment.

## Key Outcomes Confirmed

- **Warning output is materially more actionable**: the two main offline
  governance validators now explain where noise comes from instead of only
  emitting flat totals.
- **Bounded cleanup can clear the warning totals without risky rewrites**:
  follow-up batches preserved split-file authority and removed only stale
  duplicate sources or namespace extras that had been migrated.
- **The stale backlog narrative is gone**: active planning is now aligned to
  the real current debt, not the old “17 missing split slugs” snapshot.

## Residual Risks / Tech Debt

- **Large message-file diff requires careful review**: the final follow-up
  cleanup intentionally removed many non-authoritative root/base support-copy
  shadows; split files now own the runtime-visible copy.
- **TDK compliance warnings remain out of scope**: `qa:seo-governance` still
  reports `2802` warning-only length/style suggestions, which are SEO
  optimization debt rather than translation-governance failures.

## Verdict

The milestone v0.0.30 is ready for archival. All 6 requirements (TWS-01 through
TWS-06) are implemented in Phase 90. Validator ergonomics are improved,
planning/testing docs are aligned, mechanically safe cleanup batches are
complete, both translation warning channels are clear, and all focused
validation evidence is green.
