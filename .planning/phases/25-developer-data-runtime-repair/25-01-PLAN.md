# 25-01 Plan

## Goal

Rank remaining developer/data placeholder helper families and select the bounded repair wave.

## Must Be True

1. The selected family is justified by actual component imports and user-visible placeholder severity.
2. Already repaired or lower-leverage candidates are explicitly deferred.
3. The implementation scope has a bounded compatibility contract before code changes begin.

## Initial Tasks

- Confirm placeholder status and consumers for `analyzeComplexity`, `analyzeDeadCode`, `analyzePerformance`, and `analyzeQuery`.
- Select the code-analysis trio as the Phase 25 repair wave.
- Document deferred candidates: SQL query planning and broader AST/compiler-backed analysis.
- Define return-shape compatibility from the three current Svelte consumers.

## Verification

```bash
rg -n "analyzeComplexity|analyzeDeadCode|analyzePerformance|analyzeQuery" src/lib/tool-stubs.ts src/components/tools
```

## Success Criteria

- Phase 25 has a single selected helper family.
- The selected helper family has at least three representative user-facing consumers.
- Implementation can proceed without broad component rewrites.
