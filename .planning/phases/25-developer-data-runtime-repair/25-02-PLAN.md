# 25-02 Plan

## Goal

Implement the selected code-analysis helper wave with compatibility exports and targeted tests.

## Must Be True

1. `analyzeComplexity` returns line counts, function metrics, overall complexity, and maintainability data.
2. `analyzeDeadCode` returns an array of actionable unused declaration hints.
3. `analyzePerformance` returns estimated operations, hotspots, suggestions, and total time.

## Initial Tasks

- Add `src/lib/runtime-integrity/code-analysis.ts` with dependency-free deterministic helper implementations.
- Delegate the three selected exports from `src/lib/tool-stubs.ts`.
- Extend `src/lib/tool-stubs-runtime.test.ts` with representative code-analysis assertions.
- Avoid UI rewrites unless helper output reveals a narrow compatibility mismatch.

## Verification

```bash
npx vitest run src/lib/tool-stubs-runtime.test.ts
npm run check
npm run build
```

## Success Criteria

- The three selected components receive meaningful non-placeholder output for representative examples.
- Tests fail if the selected helpers regress to `{ score: 0, issues: [] }` or incompatible empty shapes.
- Phase 25 satisfies `RUNTIME-05` for the selected bounded repair wave.
