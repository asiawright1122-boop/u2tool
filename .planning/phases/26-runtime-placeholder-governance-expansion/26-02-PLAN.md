# 26-02 Plan

## Goal

Fold the new runtime evidence into the existing QA and production verification path.

## Must Be True

1. `npm run qa:runtime-integrity` runs the public runtime smoke test file.
2. `npm run qa:production` runs both runtime smoke and placeholder regression validation.
3. `npm run verify:production` inherits the new runtime governance through the canonical production gate.

## Initial Tasks

- Add `qa:runtime-integrity` to `package.json`.
- Add `validate:runtime-placeholder-regressions` to `package.json`.
- Update `qa:production` to run both runtime gates before build/release validators.
- Update validation docs if needed so the active script is no longer only documented as deprecated.

## Verification

```bash
npm run qa:runtime-integrity
npm run validate:runtime-placeholder-regressions
npm run check
npm run build
```

## Success Criteria

- Runtime placeholder regressions are release blockers.
- The gate remains deterministic and tied only to repaired helper clusters.
