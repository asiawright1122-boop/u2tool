# 26-01 Plan

## Goal

Add low-noise placeholder regression signatures for the repaired runtime clusters.

## Must Be True

1. The validator protects Phase 24 scheduling helpers and Phase 25 code-analysis helpers.
2. It still protects earlier runtime-integrity helper waves.
3. It points failures at the owning export, module, and smoke test path.

## Initial Tasks

- Restore `scripts/validation/validate-runtime-placeholder-regressions.ts` from the deprecated validator.
- Extend the protected helper list with scheduling and code-analysis exports.
- Keep checks focused on:
  - runtime import in `tool-stubs.ts`
  - exported wrapper delegation
  - runtime module export
  - placeholder fallback return shapes
  - smoke test references

## Verification

```bash
npm run validate:runtime-placeholder-regressions
```

## Success Criteria

- Validator passes on the repaired current tree.
- Reverting one repaired wrapper to `return []`, `return {}`, `return ''`, `return 0`, or `return null` would fail the audit.
