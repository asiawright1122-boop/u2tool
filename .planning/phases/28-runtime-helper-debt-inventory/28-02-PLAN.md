# 28-02 Plan

## Goal

Rank the next helper repair waves and select the bounded v0.0.9 implementation scope for Phases 29 and 30.

## Must Be True

1. Ranking uses user-visible breakage, import coverage, testability, and compatibility risk.
2. Phase 29 text utility scope is backed by inventory evidence.
3. Phase 30 validation/reference-data scope is backed by inventory evidence.

## Initial Tasks

- Review `docs/RUNTIME_HELPER_DEBT_INVENTORY.md`.
- Identify the highest-priority text utility reference-data candidates.
- Identify the highest-priority adjacent validation/reference-data candidates.
- Write `28-01-SUMMARY.md` and `28-02-SUMMARY.md` with the selected downstream scope.
- Update `.planning/STATE.md` and `.planning/ROADMAP.md` if Phase 28 completes.

## Verification

```bash
npm run report:runtime-debt
npm run planning:traceability
git diff --check
```

## Success Criteria

- Phase 29 and Phase 30 scopes are explicit enough to execute without re-ranking the whole stub surface.
- Inventory evidence points to the exact helper names and consumers for the selected waves.
- RUNTIME-06 has summary evidence and no traceability gap.
