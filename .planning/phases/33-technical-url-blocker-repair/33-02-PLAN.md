# 33-02 Plan

## Goal

Patch owned URL-shape defects only if Phase 33 live checks prove intended indexable pages are still blocked.

## Must Be True

1. If live checks find true 4xx/noindex/robots/5xx blockers on intended indexable pages, the repository-owned route/redirect/canonical defect is patched.
2. If live checks show only expected redirects/canonicals/exclusions, Phase 33 documents that no code patch is required and keeps validation paused.
3. Canonical/internal-link/search-engine validation remains green after any patch or no-op classification.

## Initial Tasks

- Review `docs/GSC_TECHNICAL_BLOCKER_LIVE_CHECK_2026-05-11.md`.
- Patch route/redirect/canonical behavior only for confirmed owned defects.
- If no patch is required, write the summary and verification evidence clearly so the user does not retry GSC validation on these rows.
- Update `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, and `.planning/TRACEABILITY.md` for Phase 33 progress.

## Verification

```bash
npm run report:gsc-technical-blockers -- --output docs/GSC_TECHNICAL_BLOCKER_LIVE_CHECK_2026-05-11.md
npm run validate:internal-link-canonicals
npm run validate:search-engine-compliance
npm run planning:traceability
git diff --check
```

## Success Criteria

- `GSC-14` is complete with live evidence and any necessary patch evidence.
- Broad GSC validation remains paused unless a coherent fixed row emerges.
