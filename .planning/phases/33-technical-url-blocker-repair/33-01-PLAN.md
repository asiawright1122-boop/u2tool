# 33-01 Plan

## Goal

Live-check high-confidence technical blocker samples from the Phase 32 `fix-before-validate` groups.

## Must Be True

1. Representative URLs from every `fix-before-validate` group are checked against live production.
2. The check records redirect chain, final status, final URL, noindex headers/meta, and canonical target where available.
3. The output distinguishes true current blockers from expected canonical/redirect states.

## Initial Tasks

- Read `docs/GSC_VALIDATION_ACTION_MATRIX_2026-05-11.md`.
- Add a live-check reporter, likely `scripts/seo/gsc-technical-blocker-live-check.ts`, that:
  - uses representative URLs from the 206 `fix-before-validate` candidates;
  - follows redirects with bounded timeout and retry behavior;
  - captures final status and indexability signals;
  - emits `docs/GSC_TECHNICAL_BLOCKER_LIVE_CHECK_2026-05-11.md`.
- Add a package script such as `report:gsc-technical-blockers`.

## Verification

```bash
npm run report:gsc-technical-blockers -- --output docs/GSC_TECHNICAL_BLOCKER_LIVE_CHECK_2026-05-11.md
git diff --check
```

## Success Criteria

- The live-check report names which `fix-before-validate` groups are true blockers versus expected monitor states.
- Any current blocker is small enough to feed directly into `33-02`.
