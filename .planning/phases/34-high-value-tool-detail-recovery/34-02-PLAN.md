# 34-02 Plan

## Goal

Patch the selected high-value Hex Editor recovery slice with accurate content, internal-link support, and rendered/content-trust evidence.

## Must Be True

1. English and Russian Hex Editor pages accurately describe the current UI and include useful FAQ support.
2. Encoding category support content creates relevant internal paths to Hex Editor and adjacent encoding tools.
3. Rendered SEO and content-trust checks cover the edited pages without adding unsupported claims.

## Initial Tasks

- Update `src/messages/en/base.json`, `src/messages/ru/base.json`, `src/messages/en/tools/hex-editor.json`, and `src/messages/ru/tools/hex-editor.json`.
- Add Phase 34 encoding category support content for English and Russian.
- Add or update tests/checks in `src/lib/category-support.test.ts`, `src/lib/support-content-fallback.test.ts`, and `scripts/validation/validate-rendered-seo.ts`.
- Run targeted tests and rendered/source validation.
- Write Phase 34 summaries, verification, and planning updates.

## Verification

```bash
npx vitest run src/lib/category-support.test.ts src/lib/support-content-fallback.test.ts
npm run i18n:check-missing-keys
npm run check
npm run build
PROD_BASE_URL=http://127.0.0.1:4321 CANONICAL_BASE_URL=https://www.u2tool.com npm run validate:rendered-seo
npm run planning:traceability
git diff --check
```

## Success Criteria

- `GSC-15` is complete for the selected Hex Editor recovery slice.
- Phase 35 can close with exact request-indexing instructions for the enhanced URLs.
