# 30-01 Plan

## Goal

Select and materialize the validation/reference-data repair slice from the inventory while keeping the current compatibility surface stable.

## Must Be True

1. The Phase 30 repair scope is limited to `commonPasswords`, `commonTypos`, `disposableDomains`, and `freeProviders`.
2. The selected helpers live in typed runtime-integrity code or populated compatibility exports.
3. Existing component imports from `@/lib/tool-stubs` continue to work.

## Initial Tasks

- Read `docs/RUNTIME_HELPER_DEBT_INVENTORY.md`, `src/components/tools/PasswordStrength.svelte`, `src/components/tools/EmailValidator.svelte`, `src/lib/tool-stubs.ts`, and `src/lib/runtime-integrity/text-reference.ts`.
- Add `src/lib/runtime-integrity/validation-reference.ts`.
- Export:
  - `commonPasswords` with representative common-password values including `password`, `123456`, `qwerty`, `admin`, `letmein`, and `password123`.
  - `commonTypos` with provider corrections including `gamil.com`, `gmial.com`, `hotmial.com`, `yaho.com`, and `outlok.com`.
  - `disposableDomains` with representative disposable domains including `mailinator.com`, `10minutemail.com`, `guerrillamail.com`, `tempmail.com`, and `yopmail.com`.
  - `freeProviders` with representative free providers including `gmail.com`, `yahoo.com`, `hotmail.com`, `outlook.com`, `icloud.com`, and `proton.me`.
- Delegate the four selected constants from `src/lib/tool-stubs.ts` to the new runtime-integrity module.

## Verification

```bash
npx vitest run src/lib/tool-stubs-runtime.test.ts
```

## Success Criteria

- The four selected helpers are no longer empty arrays or objects in the runtime compatibility path.
- The exported data shapes match the existing component usage: arrays support `.includes`/`.some`, and `commonTypos` supports direct domain-key lookup.
