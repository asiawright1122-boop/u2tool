# 29-01 Plan

## Goal

Extract or populate the selected text utility reference-data helpers while preserving `tool-stubs.ts` compatibility exports.

## Must Be True

1. Selected text helpers live in typed runtime-integrity code or populated compatibility exports.
2. Existing component imports from `@/lib/tool-stubs` continue to work.
3. Unknown-character fallback remains component-owned where the components already implement it.

## Initial Tasks

- Add a `src/lib/runtime-integrity/text-reference.ts` module for selected text reference data.
- Export ASCII art font data, Morse/NATO maps, small text maps, and flip/mirror maps.
- Delegate or re-export the selected constants from `src/lib/tool-stubs.ts`.

## Verification

```bash
npx vitest run src/lib/tool-stubs-runtime.test.ts
```

## Success Criteria

- Representative text data is non-empty and structurally compatible with current components.
- No selected helper remains an empty array/object export in `tool-stubs.ts`.
