# GSC P2 Template Cleanup Batch

Date: 2026-07-05

## Goal

Start the P2 cleanup lane after P0/P1 closure by replacing generic template copy and unsupported support claims on localized pages that still show GSC loss or coverage risk.

## Batch

| URL | Evidence | Action |
|---|---:|---|
| `/es/tools/text-repeater/` | 2 clicks / 232 impressions lost | Replace generic browser-tool SEO description with repeat count, separators, numbering, copy, and TXT download framing. |
| `/fr/tools/random-generator/` | 2 clicks / 231 impressions lost | Shorten overlong title while preserving integer range, unique option, max 1000, and copy behavior. |
| `/es/tools/fake-data-generator/` | GSC loss + generic template | Replace generic metadata and overbroad statistical/security/testing support claims with actual field, locale, editable table, JSON/CSV/SQL export behavior. |
| `/ko/tools/love-calculator/` | GSC loss + generic template | Replace algorithmic/analysis/chart claims with entertainment-only two-name percentage, message, emoji, reset, and share/copy behavior. |
| `/ja/tools/random-generator/` | impression loss + generic template | Replace generic root/base SEO description with min/max, count, unique option, max 1000, and copy framing. |
| `/es/tools/regex-generator/` | impression loss + generic template | Replace generic metadata and unsupported builder/debugger/file import claims with common patterns, custom regex, flags, test string, matches, and copy behavior. |

## Checks

- Root/base metadata remains synchronized for the six selected localized pages.
- Split support content names only controls implemented in the current Svelte components.
- No GSC request-indexing ledger changes.
- Run focused JSON/schema checks plus SEO governance after edits.
