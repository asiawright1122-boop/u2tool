---
import { getIconSvg } from '@/lib/icon-svg';
import type { TextWritingToolClusterCopy, TextWritingToolClusterGroup } from '@/lib/text-writing-tool-cluster';

interface Props {
  clusterHref: string;
  copy: TextWritingToolClusterCopy;
  currentSlug: string;
  group: TextWritingToolClusterGroup;
}

const { clusterHref, copy, currentSlug, group } = Astro.props as Props;
const siblingTools = group.tools.filter((tool) => tool.slug !== currentSlug).slice(0, 5);
---

<section
  data-text-writing-tool-cluster={group.id}
  class="glass-card mb-10 overflow-hidden p-6 md:p-8"
>
  <div class="flex flex-col gap-6 lg:flex-row lg:items-center lg:justify-between">
    <div class="max-w-4xl">
      <p class="mb-3 text-[10px] font-black uppercase tracking-[0.24em] text-emerald-600/80 dark:text-emerald-400/70">
        {copy.eyebrow}
      </p>
      <h2 class="flex items-center gap-3 text-2xl font-black tracking-tight text-slate-900 dark:text-white md:text-3xl">
        <span class="h-8 w-1 rounded-full bg-emerald-500 dark:shadow-[0_0_12px_rgba(16,185,129,0.55)]"></span>
        {group.title}
      </h2>
      <p class="mt-3 text-sm font-medium leading-7 text-slate-500 dark:text-slate-400">
        {group.description}
      </p>
    </div>

    <a
      href={clusterHref}
      class="inline-flex shrink-0 items-center justify-center rounded-2xl border border-emerald-400/40 bg-emerald-600 px-5 py-3 text-xs font-black uppercase tracking-[0.18em] text-white shadow-lg shadow-emerald-500/20 transition-all hover:-translate-y-0.5 hover:bg-emerald-700 active:translate-y-0 dark:border-emerald-300/20 dark:bg-emerald-500 dark:hover:bg-emerald-400"
    >
      {copy.ctaLabel}
    </a>
  </div>

  {siblingTools.length > 0 && (
    <div class="mt-6 flex flex-wrap gap-2">
      {siblingTools.map((tool) => (
        <a
          href={tool.href}
          data-text-writing-sibling-tool={tool.slug}
          title={tool.description || tool.name}
          class="inline-flex max-w-full items-center gap-2 rounded-full border border-slate-200 bg-white/60 px-3.5 py-2 text-xs font-bold text-slate-700 transition-all hover:border-emerald-400 hover:text-emerald-700 dark:border-slate-700 dark:bg-slate-800/60 dark:text-slate-200 dark:hover:border-emerald-500/50 dark:hover:text-emerald-300"
        >
          <span class="inline-flex shrink-0 text-slate-400 dark:text-slate-500" set:html={getIconSvg(tool.icon, 14)} />
          <span class="truncate">{tool.name}</span>
        </a>
      ))}
    </div>
  )}
</section>
