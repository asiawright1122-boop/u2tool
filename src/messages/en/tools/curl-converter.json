{
  "detailed_description": "The cURL Converter turns a pasted curl command into equivalent request code in JavaScript (fetch), Python (requests), PHP (curl_setopt), Go (net/http), or Java (java.net.http). A small regex-based parser reads the `-X` method, `-H` headers, and `-d` or `--data` body from the command and emits a template snippet for the chosen language, which you can copy from the output area. The tool covers basic HTTP requests only and does not handle cookies, `--cookie-jar`, file uploads, multipart forms, `-u` basic auth, client certificates, `-L` follow-redirect, query string flags such as `-G`, Ruby or Node Axios output, or syntax highlighting and downloadable files.",
  "usage_steps": [
    "Paste a curl command into the textarea, including the URL and any `-X`, `-H`, or `-d` flags.",
    "Pick an output language from the dropdown: JavaScript, Python, PHP, Go, or Java.",
    "Click Convert to generate the request code from the parsed method, headers, and body.",
    "Review the generated code in the output panel.",
    "Click Copy Code to copy the snippet to your clipboard for manual review."
  ],
  "usage_examples": [
    "Convert a simple `curl -X POST -H 'Content-Type: application/json' -d '{...}'` command into a JavaScript `fetch` snippet.",
    "Generate a Python `requests` call from a curl command with a Bearer authorization header.",
    "Translate a GET request with custom headers into a Go `net/http` example.",
    "Produce a Java `HttpClient` snippet from a curl POST as a starting point before manual edits."
  ],
  "faqs": [
    {
      "question": "Is the cURL Converter free?",
      "answer": "Yes. The converter is completely free with no signup, and there is no limit on how many commands you can convert."
    },
    {
      "question": "Is it safe to paste curl commands here?",
      "answer": "Yes. Parsing and code generation run entirely in your browser — the command is never uploaded to any server, so tokens in Authorization headers stay on your device."
    },
    {
      "question": "Which curl flags are supported?",
      "answer": "The parser reads the request URL, the `-X` method flag, `-H` headers, and `-d` or `--data` bodies. Cookies, file uploads, multipart forms, basic auth and redirect flags are outside the current scope."
    },
    {
      "question": "Which output languages can I choose?",
      "answer": "You can convert a curl command into JavaScript (fetch), Python (requests), PHP (curl_setopt), Go (net/http), or Java (java.net.http) snippets."
    },
    {
      "question": "Can I use the generated code directly in production?",
      "answer": "Treat the output as a starting template. It covers the basic request shape — review timeouts, error handling and authentication for your environment before shipping."
    }
  ]
}