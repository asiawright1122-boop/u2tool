{
  "detailed_description": "The JSON Diff tool compares two JSON values in the browser and lists changed, added, removed, and type-different paths. Paste the original JSON on the left, paste the changed JSON on the right, and run the comparison to inspect nested object and array differences. It is designed for quick API response checks, configuration reviews, and debugging small to medium JSON payloads. The tool expects valid JSON on both sides and reports parsing errors when the input is not valid JSON.",
  "usage_steps": [
    "Paste the original JSON into the first editor.",
    "Paste the updated JSON into the second editor.",
    "Click Compare to parse both inputs.",
    "Review each reported path and difference type.",
    "Fix invalid JSON if the parser reports an error.",
    "Copy or use the findings in your review workflow."
  ],
  "usage_examples": [
    "Compare two API responses after a backend change.",
    "Find which keys changed between two configuration files.",
    "Check whether a nested value changed type from string to number.",
    "Review a small JSON fixture before updating a test."
  ],
  "faqs": [
    {
      "question": "What kinds of differences are shown?",
      "answer": "The page reports added keys, removed keys, changed values, and type differences at their JSON paths."
    },
    {
      "question": "Does it accept invalid JSON?",
      "answer": "No. Both inputs must be valid JSON before the comparison can run."
    },
    {
      "question": "Can it compare arrays?",
      "answer": "Yes. Arrays are parsed as JSON values, but large reordered arrays may produce many path-level changes."
    },
    {
      "question": "Is this a merge tool?",
      "answer": "No. It highlights differences; it does not automatically merge the two JSON documents."
    },
    {
      "question": "Does it upload my JSON?",
      "answer": "The comparison runs in the browser page and does not require account login."
    }
  ]
}
